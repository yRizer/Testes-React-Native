import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        width: '100%',
        height: 52,
        borderRadius: 10,
        borderWidth: 1,
        borderColor: '#3f3f46',
        color: '#fff',
        backgroundColor: '#323232ff',
    }
})